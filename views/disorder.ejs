<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Log Data</title>
  <!-- Bootstrap CSS link -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      padding: 20px;
    }
    table {
      width: 100%;
      margin-top: 20px;
    }
    th, td {
      padding: 10px;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mt-4 mb-4">Order Data</h1>
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>Order ID</th>
          <th>User ID</th>
          <th>Worker ID</th>
          <th>Hours</th>
          <th>Address</th>
          <th>City</th>
          <th>Date</th>
        </tr>
      </thead>
      <tbody>
        <% result.forEach(function(entry) { %>
          <tr>
            <td><%= entry.oid %></td>
            <td><%= entry.id %></td>
            <td><%= entry.wid %></td>
            <td><%= entry.hours %></td>
            <td><%= entry.address %></td>
            <td><%= entry.city %></td>
            <td><%= entry.odate %></td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  </div>
  <!-- Bootstrap JS and Popper.js scripts (for Bootstrap features) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>